# コピーして .env にリネームして使う
# 本番では必ず SECRET_KEY を固有の値に差し替えること

# Django
# SECRET_KEY=your-secret-key-here
# DEBUG=False
# ALLOWED_HOSTS=yourdomain.com,www.yourdomain.com

# DB（省略すると SQLite を使用）
# DATABASE_URL=postgres://user:password@localhost:5432/kakeibo_regret
